rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Anyone can read any document
      allow read: if true;
      // Any authenticated user can write to any document
      allow write: if request.auth != null;
    }
  }
}
