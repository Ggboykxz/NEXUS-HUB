/**
 * FICHIER DÉSACTIVÉ - ROUTAGE CONSOLIDÉ À LA RACINE src/app/webtoon-hub/page.tsx
 * Cela résout l'erreur Next.js "You cannot have two parallel pages that resolve to the same path".
 */
export default function Page() {
  return null;
}
