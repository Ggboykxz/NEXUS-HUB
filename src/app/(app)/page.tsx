/**
 * Fichier neutralisé pour éviter les conflits de routes avec src/app/page.tsx.
 * La page d'accueil principale est gérée à la racine pour garantir la cohérence du déploiement.
 */
export default function AppHomeRedirect() {
  return null;
}
